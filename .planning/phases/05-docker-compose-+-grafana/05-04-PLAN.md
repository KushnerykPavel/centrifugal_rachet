---
phase: 05-docker-compose-+-grafana
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - grafana/dashboards/ratchet.json
  - cmd/alice-classical/Dockerfile
  - cmd/bob-classical/Dockerfile
  - cmd/alice-pq/Dockerfile
  - cmd/bob-pq/Dockerfile
  - docker-compose.yml
autonomous: true
gap_closure: true
requirements:
  - OBS-05
  - DEPL-03

must_haves:
  truths:
    - "Stat panel 1 (Classical) in ratchet.json queries ratchet_message_wire_bytes and has unit: bytes"
    - "Stat panel 2 (PQ) in ratchet.json queries ratchet_message_wire_bytes and has unit: bytes"
    - "All four Dockerfiles use golang:1.24-alpine (not golang:1.25-alpine)"
    - "docker-compose.yml sets GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer (not Admin)"
  artifacts:
    - path: "grafana/dashboards/ratchet.json"
      provides: "Corrected dashboard with wire-byte Stat panels"
      contains: "ratchet_message_wire_bytes"
    - path: "cmd/alice-classical/Dockerfile"
      provides: "Buildable multi-stage Dockerfile"
      contains: "golang:1.24-alpine"
    - path: "cmd/bob-classical/Dockerfile"
      provides: "Buildable multi-stage Dockerfile"
      contains: "golang:1.24-alpine"
    - path: "cmd/alice-pq/Dockerfile"
      provides: "Buildable multi-stage Dockerfile"
      contains: "golang:1.24-alpine"
    - path: "cmd/bob-pq/Dockerfile"
      provides: "Buildable multi-stage Dockerfile"
      contains: "golang:1.24-alpine"
    - path: "docker-compose.yml"
      provides: "Seven-service stack with correct Grafana role"
      contains: "GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer"
  key_links:
    - from: "Stat panel 1 expr"
      to: "ratchet_message_wire_bytes histogram"
      via: "PromQL rate(sum)/rate(count)"
      pattern: "ratchet_message_wire_bytes_sum.*alice-classical|bob-classical"
    - from: "Stat panel 2 expr"
      to: "ratchet_message_wire_bytes histogram"
      via: "PromQL rate(sum)/rate(count)"
      pattern: "ratchet_message_wire_bytes_sum.*alice-pq|bob-pq"
---

<objective>
Close three gaps found by verification (05-VERIFICATION.md) and code review (05-REVIEW.md):

1. GAP 1 (OBS-05 BLOCKER): Stat panels in ratchet.json query handshake latency, not wire bytes. Fix by rewriting both Stat panel PromQL expressions to query ratchet_message_wire_bytes and setting unit to bytes.
2. GAP 2 (CR-01 CRITICAL): All four Dockerfiles use golang:1.25-alpine which does not exist as a Docker image. Fix by changing to golang:1.24-alpine.
3. GAP 3 (CR-02 SECURITY): docker-compose.yml grants anonymous Grafana users Admin role. Fix by changing to Viewer.

Purpose: Unblock docker compose build (CR-01), reduce attack surface (CR-02), and satisfy ROADMAP SC3 + OBS-05 wire-byte comparison (GAP 1).
Output: Six corrected files ready for re-verification.
</objective>

<execution_context>
@/Users/pavelkushneryk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pavelkushneryk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-docker-compose-+-grafana/05-VERIFICATION.md
@.planning/phases/05-docker-compose-+-grafana/05-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Stat panels in ratchet.json to show wire bytes</name>
  <read_first>grafana/dashboards/ratchet.json, internal/metrics/metrics.go</read_first>
  <files>grafana/dashboards/ratchet.json</files>
  <action>
Rewrite the two Stat panels (id: 1 and id: 2) in grafana/dashboards/ratchet.json. The histogram ratchet_message_wire_bytes has no labels other than those injected by Prometheus scrape config (job). No msg_type label exists — the metric is a flat histogram. Use the same job filter pattern that already works in the Time Series wire-size panel (panel id: 3).

IMPORTANT: The histogram's _sum and _count series names are ratchet_message_wire_bytes_sum and ratchet_message_wire_bytes_count. The PromQL divides rate of sum by rate of count to get the rolling average bytes per message.

Panel id 1 — Classical Handshake Wire Size:
- title: change from "Classical Handshake" to "Classical Wire Size (avg bytes)"
- fieldConfig.defaults.unit: change from "s" to "bytes"
- fieldConfig.defaults.thresholds.steps: replace with [{"color":"green","value":null},{"color":"yellow","value":500},{"color":"red","value":2000}]
- targets[0].expr: replace the entire expr string with exactly:
  rate(ratchet_message_wire_bytes_sum{job=~"alice-classical|bob-classical"}[1m]) / rate(ratchet_message_wire_bytes_count{job=~"alice-classical|bob-classical"}[1m])
- targets[0].legendFormat: keep "{{job}}"
- targets[0].refId: keep "A"

Panel id 2 — PQ Handshake Wire Size:
- title: change from "PQ Handshake" to "PQ Wire Size (avg bytes)"
- fieldConfig.defaults.unit: change from "s" to "bytes"
- fieldConfig.defaults.thresholds.steps: replace with [{"color":"green","value":null},{"color":"yellow","value":500},{"color":"red","value":2000}]
- targets[0].expr: replace the entire expr string with exactly:
  rate(ratchet_message_wire_bytes_sum{job=~"alice-pq|bob-pq"}[1m]) / rate(ratchet_message_wire_bytes_count{job=~"alice-pq|bob-pq"}[1m])
- targets[0].legendFormat: keep "{{job}}"
- targets[0].refId: keep "A"

All other panels (id: 3, 4, 5), all other fields, the uid, version, refresh, time window, tags, and all datasource references remain unchanged.

Write the complete corrected JSON file using the Write tool. Preserve the existing indentation and JSON formatting style (2-space indent).
  </action>
  <verify>
    <automated>grep -c "ratchet_message_wire_bytes_sum" grafana/dashboards/ratchet.json</automated>
  </verify>
  <acceptance_criteria>
- grep -c "ratchet_message_wire_bytes_sum" grafana/dashboards/ratchet.json outputs 4 (2 from the new Stat panels + 2 already in Time Series panel 3 that use rate([30s]))
- grep '"unit": "s"' grafana/dashboards/ratchet.json outputs 2 (only the two latency Time Series panels, ids 4 and 5 — NOT the Stat panels)
- grep '"unit": "bytes"' grafana/dashboards/ratchet.json outputs 2 (the wire-size Time Series panel id 3, plus one of the Stat panels — actually both Stat panels now get "bytes" so outputs 3 total: panels 1, 2, and 3)
- grep "ratchet_handshake_duration_seconds" grafana/dashboards/ratchet.json outputs 0 (the Stat panels no longer reference this metric; only latency Time Series panels use it but under ratchet_encrypt/decrypt names — actually handshake_duration_seconds should appear 0 times in Stat panels. Verify total grep count drops from the original which had 2 expr referencing it in Stat panels)
- grep -c '"title": "Classical Wire Size' grafana/dashboards/ratchet.json outputs 1
- grep -c '"title": "PQ Wire Size' grafana/dashboards/ratchet.json outputs 1
- python3 -c "import json,sys; d=json.load(open('grafana/dashboards/ratchet.json')); print('valid json')" prints "valid json" (file remains valid JSON)
  </acceptance_criteria>
  <done>Both Stat panels query ratchet_message_wire_bytes_sum/count with job filter, unit is bytes, titles reflect wire size. JSON is valid. The latency Time Series panels (ids 4 and 5) are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Dockerfiles — change golang:1.25-alpine to golang:1.24-alpine</name>
  <read_first>cmd/alice-classical/Dockerfile, cmd/bob-classical/Dockerfile, cmd/alice-pq/Dockerfile, cmd/bob-pq/Dockerfile</read_first>
  <files>cmd/alice-classical/Dockerfile, cmd/bob-classical/Dockerfile, cmd/alice-pq/Dockerfile, cmd/bob-pq/Dockerfile</files>
  <action>
In each of the four Dockerfiles, change line 1 from:
  FROM golang:1.25-alpine AS builder
to:
  FROM golang:1.24-alpine AS builder

The rest of each Dockerfile (WORKDIR, COPY, RUN go mod download, COPY . ., RUN CGO_ENABLED=0 go build, the runtime stage FROM alpine:latest, apk add, COPY binary, EXPOSE, HEALTHCHECK, CMD) remains exactly as-is.

Write each Dockerfile using the Write tool, replacing only the base image tag on line 1. The four files are:
- cmd/alice-classical/Dockerfile
- cmd/bob-classical/Dockerfile
- cmd/alice-pq/Dockerfile
- cmd/bob-pq/Dockerfile

Note: The go.mod in Phase 1 was bumped to Go 1.25.0 minimum by go-doubleratchet, but Go 1.24 toolchain can still satisfy module requirements when the module declares go 1.25 — the builder image must actually exist on Docker Hub. golang:1.25-alpine does not exist; golang:1.24-alpine does. This is the correct fix per CR-01.
  </action>
  <verify>
    <automated>grep "FROM golang:" cmd/alice-classical/Dockerfile cmd/bob-classical/Dockerfile cmd/alice-pq/Dockerfile cmd/bob-pq/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
- grep "FROM golang:1.25-alpine" cmd/alice-classical/Dockerfile cmd/bob-classical/Dockerfile cmd/alice-pq/Dockerfile cmd/bob-pq/Dockerfile exits with no output (no matches)
- grep "FROM golang:1.24-alpine" cmd/alice-classical/Dockerfile cmd/bob-classical/Dockerfile cmd/alice-pq/Dockerfile cmd/bob-pq/Dockerfile outputs 4 lines (one per file)
- wc -l cmd/alice-classical/Dockerfile outputs 14 (line count unchanged — only line 1 content changed)
- wc -l cmd/bob-classical/Dockerfile outputs 14
- wc -l cmd/alice-pq/Dockerfile outputs 14
- wc -l cmd/bob-pq/Dockerfile outputs 14
  </acceptance_criteria>
  <done>All four Dockerfiles have golang:1.24-alpine on line 1. No other lines changed. docker compose build will no longer fail due to a non-existent image tag.</done>
</task>

<task type="auto">
  <name>Task 3: Fix docker-compose.yml — reduce anonymous Grafana role from Admin to Viewer</name>
  <read_first>docker-compose.yml</read_first>
  <files>docker-compose.yml</files>
  <action>
In docker-compose.yml, find the Grafana service environment section and change the value of GF_AUTH_ANONYMOUS_ORG_ROLE from Admin to Viewer.

The exact line to change (currently line 96) is:
  - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin

Change it to:
  - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer

Write the complete docker-compose.yml using the Write tool. Read the full file first, make only this single-value change, and write back the entire file without altering any other line (service definitions, depends_on, health checks, port mappings, volumes, environment variables for other services, etc.).

Optionally add an inline comment on the same line for clarity:
  - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer  # read-only; Admin not needed for local demo
but only if the surrounding lines use the same comment style. Do not add a comment if no other environment lines have comments — keep style consistent.
  </action>
  <verify>
    <automated>grep "GF_AUTH_ANONYMOUS_ORG_ROLE" docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
- grep "GF_AUTH_ANONYMOUS_ORG_ROLE=Admin" docker-compose.yml exits with no output (no matches)
- grep "GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer" docker-compose.yml outputs 1 line
- grep -c "condition: service_healthy" docker-compose.yml outputs 8 (unchanged — no other lines affected)
- python3 -c "import sys,subprocess; r=subprocess.run(['docker','compose','-f','docker-compose.yml','config','--quiet'],capture_output=True); print('valid' if r.returncode==0 else r.stderr.decode())" prints "valid" — OR if docker not available: grep -c "services:" docker-compose.yml outputs 1 (file is still valid YAML structure)
  </acceptance_criteria>
  <done>docker-compose.yml has GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer. Anonymous users can view dashboards but cannot edit datasources, delete dashboards, or manage users. All other compose configuration unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → Grafana port 3000 | Unauthenticated HTTP from any host-network client to Grafana |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation of Privilege | Grafana anonymous role | mitigate | Change GF_AUTH_ANONYMOUS_ORG_ROLE from Admin to Viewer (Task 3) — prevents unauthenticated datasource modification and dashboard deletion |
| T-05-04-02 | Spoofing | Docker build supply chain | accept | golang:1.24-alpine is an official Docker Hub image; pinning to a minor version (not patch) is standard for Go builder images in demo projects; full SHA pinning is out of scope for a local demo |
</threat_model>

<verification>
After all three tasks complete, run these checks in order:

1. JSON validity:
   python3 -c "import json; json.load(open('grafana/dashboards/ratchet.json')); print('OK')"

2. Stat panel metric check:
   grep -c "ratchet_message_wire_bytes_sum" grafana/dashboards/ratchet.json
   Expected: 4 (2 Stat panels + 2 targets in Time Series panel 3)

3. No latency metric in Stat panels:
   python3 -c "
   import json
   d = json.load(open('grafana/dashboards/ratchet.json'))
   stat_panels = [p for p in d['panels'] if p['type']=='stat']
   for p in stat_panels:
       for t in p['targets']:
           assert 'handshake_duration' not in t['expr'], f'Stat panel {p[\"id\"]} still uses latency: {t[\"expr\"]}'
           assert 'ratchet_message_wire_bytes' in t['expr'], f'Stat panel {p[\"id\"]} missing wire bytes: {t[\"expr\"]}'
       assert p['fieldConfig']['defaults']['unit'] == 'bytes', f'Stat panel {p[\"id\"]} unit is not bytes'
   print('Stat panels OK')
   "

4. Dockerfile base image:
   grep "FROM golang:" cmd/alice-classical/Dockerfile cmd/bob-classical/Dockerfile cmd/alice-pq/Dockerfile cmd/bob-pq/Dockerfile
   Expected: all four show golang:1.24-alpine, none show golang:1.25-alpine

5. Grafana role:
   grep "GF_AUTH_ANONYMOUS_ORG_ROLE" docker-compose.yml
   Expected: GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
</verification>

<success_criteria>
- ratchet.json Stat panel 1: expr queries ratchet_message_wire_bytes_sum/count filtered to job=~"alice-classical|bob-classical", unit="bytes"
- ratchet.json Stat panel 2: expr queries ratchet_message_wire_bytes_sum/count filtered to job=~"alice-pq|bob-pq", unit="bytes"
- ratchet.json is valid JSON (python3 -c "import json; json.load(open(...))" succeeds)
- All four Dockerfiles: FROM golang:1.24-alpine AS builder on line 1
- docker-compose.yml: GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
- No other lines in any of the six files were changed
</success_criteria>

<output>
After completion, create `.planning/phases/05-docker-compose-+-grafana/05-04-SUMMARY.md`
</output>
