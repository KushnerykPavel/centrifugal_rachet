---
phase: 06-readme-blog-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DEPL-04

must_haves:
  truths:
    - "A reader can clone the repo and run the system with two commands: git clone + docker compose up"
    - "README comparison table shows handshake and ratchet step byte sizes (marked as measured or TODO-VERIFY if unconfirmed)"
    - "ASCII message-flow diagrams for X3DH and PQXDH appear in the README (actors: Alice, Centrifugo, Bob; arrows show what message flows when)"
    - "Code pointers section links to internal/classical/classical.go, internal/pq/pq.go, internal/metrics/metrics.go — no inline snippets"
    - "Architecture overview captures all 7 services with their host ports"
    - "Blog draft link appears as the final README item per D-02"
  artifacts:
    - path: "README.md"
      provides: "Operator-facing quick-start and comparison reference"
      min_lines: 150
  key_links:
    - from: "README.md#quick-start"
      to: "docker-compose.yml"
      via: "`docker compose up` command"
      pattern: "docker compose up"
    - from: "README.md#comparison-table"
      to: "REQUIREMENTS.md DEPL-04 numbers"
      via: "table rows with byte values"
      pattern: "1208|1128"
---

<objective>
Create README.md at the repo root: a lean operator guide (~200 lines) that gets a reader from clone to running system with verified numbers.

Purpose: Satisfy DEPL-04 — every person who lands on the repo can immediately run it and understand the wire-size difference.
Output: README.md with 9 sections per D-04, message-flow ASCII diagrams per D-05, file links per D-07, blog link per D-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-readme-blog-content/06-CONTEXT.md
@.planning/phases/06-readme-blog-content/06-RESEARCH.md

<interfaces>
<!-- Verified service topology from docker-compose.yml -->
Services and ports:
  centrifugo  :8000   (pub/sub hub, ch-classical + ch-pq)
  bob-classical :9092  (depends_on centrifugo healthy)
  alice-classical :9091 (depends_on centrifugo + bob-classical healthy)
  bob-pq :9094        (depends_on centrifugo healthy)
  alice-pq :9093      (depends_on centrifugo + bob-pq healthy)
  prometheus :9090    (depends_on centrifugo healthy)
  grafana :3000       (depends_on centrifugo healthy; anonymous Viewer, no login)

<!-- Verified source file paths for code pointers section -->
internal/classical/classical.go   — X3DH handshake facade + DR session
internal/pq/pq.go                 — PQXDH handshake facade + Triple Ratchet session
internal/pq/provider.go           — MLKEMProvider (scka.Provider, ML-KEM-768)
internal/metrics/metrics.go       — Prometheus histograms
internal/protocol/envelope.go     — Envelope, type constants, channel names
prometheus/prometheus.yml         — scrape config with protocol/role labels
grafana/provisioning/             — dashboard provisioning YAML
docker-compose.yml                — 7-service topology

<!-- Comparison numbers status — LOW confidence, must be marked for verification -->
Handshake: classical ~120 bytes vs PQ ~1208 bytes (from REQUIREMENTS.md; NOT confirmed by live run)
Ratchet step: classical ~40 bytes vs PQ ~1128 bytes (from REQUIREMENTS.md; NOT confirmed by live run)
ML-KEM-768 encap key: 1184 bytes (HIGH confidence — provider.go constant)
ML-KEM-768 ciphertext: 1088 bytes (HIGH confidence — provider.go constant)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with all 9 sections per D-04</name>
  <read_first>
    - .planning/phases/06-readme-blog-content/06-CONTEXT.md (decisions D-01 through D-08, section order D-04)
    - .planning/phases/06-readme-blog-content/06-RESEARCH.md (ASCII diagrams, service topology table, pitfalls)
    - docker-compose.yml (ports, service names, restart policies — authoritative for architecture section)
    - internal/classical/classical.go (file path for code pointers section)
    - internal/pq/pq.go (file path for code pointers section)
    - internal/metrics/metrics.go (file path for code pointers section)
  </read_first>
  <files>README.md</files>
  <action>
Create README.md at the repo root. Target ~200 lines. Sections in this exact order (per D-04):

1. **Project name + one-line description**
   "# Centrifugal Ratchet"
   "A side-by-side comparison of classical (X3DH + Double Ratchet) and post-quantum (PQXDH + Triple Ratchet) encrypted chat over Centrifugo pub/sub, with live Prometheus metrics and a Grafana dashboard."

2. **Prerequisites**
   - Docker Engine 20.10+ (when `docker compose` became a first-class plugin — no hyphen variant)
   - docker compose v2+ (bundled with Docker Desktop or install separately)
   - Note: No local Go installation needed — all builds happen inside Docker containers.

3. **Quick-start** (per DEPL-04)
   ```bash
   git clone https://github.com/KushnerykPavel/centrifugal-ratchet.git
   cd centrifugal-ratchet
   docker compose up
   ```
   Note: First run builds four Go binaries inside Docker — takes 1-2 minutes.

4. **What you'll see**
   Brief description: centrifugo starts first; bob-classical and bob-pq publish prekey bundles; alice-classical and alice-pq complete X3DH / PQXDH handshakes; both pairs exchange encrypted messages; Grafana dashboard at http://localhost:3000 shows wire-size and latency panels. Client binaries exit after the echo exchange (restart: on-failure — expected).

5. **Comparison table** (per DEPL-04)
   Use a markdown table. Mark numbers with footnote `*` = "measured from a live docker compose up run; update if your values differ."

   | Metric | Classical (X3DH + DR) | Post-Quantum (PQXDH + TR) |
   |--------|----------------------|--------------------------|
   | Handshake initial message | ~120 bytes* | ~1208 bytes* |
   | Ratchet step message | ~40 bytes* | ~1128 bytes* |
   | ML-KEM-768 encap key | — | 1184 bytes |
   | ML-KEM-768 ciphertext | — | 1088 bytes |

   `* measured from a live \`docker compose up\` run; verify with Grafana at http://localhost:3000`

6. **Architecture overview**
   Use a markdown table listing all 7 services (service name, host port, purpose) — Claude's discretion per CONTEXT.md. Follow the verified topology from docker-compose.yml:
   centrifugo :8000, bob-classical :9092, alice-classical :9091, bob-pq :9094, alice-pq :9093, prometheus :9090, grafana :3000.

7. **ASCII sequence diagrams** (per D-05 — message-flow-only, no crypto internals)

   Include BOTH diagrams from RESEARCH.md exactly as written:
   - X3DH message flow: Alice | Centrifugo (ch-classical) | Bob with arrows for prekey_bundle, initial_msg, ratchet_msg
   - PQXDH message flow: Alice | Centrifugo (ch-pq) | Bob with arrows including "(contains ML-KEM ciphertext)" note

8. **Code pointers** (per D-07 — file links only, NO copy-pasted snippets)
   Format as a bullet list of markdown links:
   - Key exchange (classical): [internal/classical/classical.go](internal/classical/classical.go)
   - Key exchange (PQ): [internal/pq/pq.go](internal/pq/pq.go)
   - ML-KEM provider: [internal/pq/provider.go](internal/pq/provider.go)
   - Prometheus metrics: [internal/metrics/metrics.go](internal/metrics/metrics.go)
   - Message envelope: [internal/protocol/envelope.go](internal/protocol/envelope.go)
   - Scrape config (protocol/role labels): [prometheus/prometheus.yml](prometheus/prometheus.yml)
   - Service topology: [docker-compose.yml](docker-compose.yml)

9. **Blog link** (per D-02, exactly this text)
   `📝 Blog post draft: [blog-draft.md](blog-draft.md)`

Tone: run-it + numbers, minimal narrative. No hand-holding on crypto fundamentals. Audience knows TLS basics (per D-03).
  </action>
  <verify>
    <automated>grep -c "docker compose up" README.md && grep -c "localhost:3000" README.md && grep -c "1208" README.md && grep -c "blog-draft.md" README.md && grep -c "internal/classical/classical.go" README.md && grep -c "ASCII\|prekey_bundle\|initial_msg\|ratchet_msg" README.md</automated>
  </verify>
  <done>
    README.md exists at repo root with all 9 sections. grep confirms: "docker compose up" present, "localhost:3000" present, "1208" present in comparison table, "blog-draft.md" link present, "internal/classical/classical.go" link present, ASCII diagram keywords present. File is 150+ lines. No inline code snippets — only file links in code pointers section.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → reader | Documentation-only; no executable code, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | README comparison table | accept | Numbers are measurement estimates marked with footnote — no false precision claimed |
| T-06-01-02 | Spoofing | README git clone URL | accept | URL points to public repo; no credentials involved |
</threat_model>

<verification>
After task completion:
1. `wc -l README.md` — must be 150+ lines
2. `grep "docker compose up" README.md` — must match (DEPL-04 quick-start)
3. `grep "1208" README.md` — must match (comparison table PQ handshake bytes)
4. `grep "1128" README.md` — must match (comparison table PQ ratchet step)
5. `grep "blog-draft.md" README.md` — must match (D-02 blog link)
6. `grep "internal/classical/classical.go" README.md` — must match (D-07 code pointer)
7. `grep -v "\`\`\`go" README.md | grep "InitiatorHandshake\|Encrypt\|Decrypt"` — must return 0 lines (no inline snippets, per D-07)
</verification>

<success_criteria>
README.md exists at repo root. Running `docker compose up` after `git clone` is the complete quick-start. Comparison table has both protocol rows with byte numbers footnoted for live verification. Both ASCII message-flow diagrams are present. Code pointers section has 7 file links and zero inline code snippets. Blog link is the last item. File is ~200 lines.
</success_criteria>

<output>
After completion, create `.planning/phases/06-readme-blog-content/06-01-SUMMARY.md` following the summary template.
</output>
